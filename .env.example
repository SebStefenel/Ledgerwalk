# Copy to .env (gitignored). Phase 1 needs none of these.
# ANTHROPIC_API_KEY=
# Per-service credentials are referenced by key name only — the model never sees
# a value, it emits e.g. NOTION_PASSWORD and the executor resolves it at fill time.
# NOTION_EMAIL=
# NOTION_PASSWORD=
# NOTION_TOTP_SECRET=
